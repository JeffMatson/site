---
---

<div class="wrapper sanityModeHidden">
    <p class="inner">
        <slot />    
    </p>
</div>

<style lang="scss">
    @import '../styles/config/variables';

    .wrapper {
        width: 100%;
        white-space: nowrap;
        overflow: hidden;
        box-sizing: border-box;
    }

    .inner {
        display: inline-block;
        padding-left: 100%;
        animation: marquee 15s linear infinite;
    }

    @keyframes marquee {
        0%   { transform: translate(0, 0); }
        100% { transform: translate(-100%, 0); }
    }
</style>
